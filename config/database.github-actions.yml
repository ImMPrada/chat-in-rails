default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: chat_in_rails_development

test:
  <<: *default
  database: chat_in_rails_development_test
